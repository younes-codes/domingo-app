import {YearSummaryDirective} from './year-summary.directive';

describe('SummaryDirectiveDirective', () => {
    it('should create an instance', () => {
        const directive = new YearSummaryDirective();
        expect(directive).toBeTruthy();
    });
});
